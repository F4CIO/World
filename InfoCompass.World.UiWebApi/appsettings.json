{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  //This setting is relevant when running behind a reverse proxy or load balancer to ensure that only certain known hosts are allowed to forward requests to the application
  "AllowedHosts": "*" 
}
